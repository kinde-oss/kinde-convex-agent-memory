# Example app

A minimal Convex app that installs the memory component via `convex/convex.config.ts` — just enough to typecheck and prove the component mounts (see `convex/setup.test.ts`). The real reference app, exercising the governed memory surface end to end, arrives in P9.

Run the tests with `npm test` from the repo root.
